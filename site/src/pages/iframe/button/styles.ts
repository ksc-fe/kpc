import {css} from '@emotion/css';

export function makeStyles() {
    return css`
        display: flex;
        justify-content: center;
        .k-btn {
            height: 40px;
            font-size: 16px;
        }
    `
}
