
const {width, height} = this.get();

<div>
    <iframe 
        ref={this.frameRef} 
        src={this.get('href')} 
        frameborder="0"
        width={width == 0 ? 'auto' : width}
        height={height == 0 ? 'auto' : height}
    ></iframe>
</div>