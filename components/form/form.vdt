const {children, className, ref, key, style, layout} = self.get();

const classNameObj = {
    'k-form': true,
    [className]: className,
    [`k-${layout}`]: layout,
};

<form class={{ classNameObj }} style={{ style }} 
    ev-submit={{ self._submit }}
    noValidate
>{{ children }}</form>
