const {children, className, ref, key, style} = self.get();

const classNameObj = {
    'k-form': true,
    [className]: className
};

<form class={{ classNameObj }} style={{ style }} 
    ev-submit={{ self._submit }}
    noValidate
>{{ children }}</form>
