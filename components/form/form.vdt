const {children, className, ref, key, layout, ...rest} = self.get();

const classNameObj = {
    'k-form': true,
    [className]: className,
    [`k-${layout}`]: layout,
};

<form {{ ...rest }} class={{ classNameObj }}
    ev-submit={{ self._submit }}
    noValidate
>{{ children }}</form>
