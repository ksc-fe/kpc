const FormItem = require('./formItem');

let {key, name, children, ...rest} = self.get();
if (!key) {
    key = `$formItem.${name}`;
}

<FormItem key={{ key }} name={{ name }} {{ ...rest }}>
    {{ children }}
</FormItem>
