@require('../../styles/themes/default.styl')

$transfer-panel-width := unit(300 / 14, rem)
$transfer-panel-height := unit(300 / 14, rem)
$transfer-title-color := $title-color
$transfer-title-line-height := unit(30 / 14, rem)
$transfer-panel-border := 1px solid $border-color
$transfer-panel-border-radius := $border-radius
$transfer-item-color := $text-color
$transfer-item-bg-color := #fff
$transfer-item-hover-bg-color := $bg-color
$transfer-item-padding := 5px 10px

// arrow
$transfer-arrow-margin := 10px

