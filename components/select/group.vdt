const {children, label, _card, className} = self.get();

<div class={{ {"k-group": true, [className]: className} }}>
    <div class="k-label" v-if={{ !_card }}>
        <b:label>{{ label }}</b:label>
    </div>
    {{ children }}
</div>
