const {className, vertical, children, fluid} = this.get();

const classNameObj = {
    'k-btns': true,
    'k-vertical': vertical,
    'k-fluid': fluid,
    [className]: className,
};

<div class={classNameObj}>{children}</div>
