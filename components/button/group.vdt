import {getRestProps} from '../utils';
import makeStyles from './groupStyles';

const {className, vertical, children, fluid} = this.get();

const classNameObj = {
    'k-btns': true,
    'k-vertical': vertical,
    'k-fluid': fluid,
    [className]: className,
    [makeStyles()]: true,
};

<div class={classNameObj} {...getRestProps(this)}>{children}</div>
