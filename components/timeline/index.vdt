import {mapChildren} from '../utils';

const {className, children, key, ref, ...rest} = self.get();

const classNameObj = {
    'k-timeline': true,
    [className]: className,
};

<div class={{ classNameObj }} {{ ...rest }}>
    {{ mapChildren(children, vNode => {
        return vNode;
    }) }}
</div>
