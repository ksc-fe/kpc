@require('../../styles/themes/default.styl')

$editable-icon-margin := 0 0 0 unit(8 / 14, em)
$editable-line-height := unit(30 / 14, em) 

// invalid
$editable-invalid-border := 1px solid $danger-color
