const {className, children, style, key, ref, ...rest} = self.get();

const __className = self.get('_className');

const classNameObj = {
    ...__className,
    [className]: className,
};

<div {{ ...rest }} class={{ classNameObj }}>{{ children }}</div>
