import Spin from '../spin';

<div class="k-app">
    {{ self.get('view') }}
    <b:loading>
        <Animate key="loading" v-if={{ self.get('loading') }}
            a:transition="fade"
            class="k-app-loading"
        >
            <Spin size="large" />
        </Animate>
    </b:loading>
</div>
