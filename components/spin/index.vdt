const {size, center} = self.get();

const classNameObj = {
    'k-spin': true,
    [`k-${size}`]: size !== 'default',
    'k-center': center,
};

<Animate class={{ classNameObj }}
    a:transition="fade"
>
    <div class="k-canvas">
        <b:canvas>
            <svg viewBox="0 0 120 120">
                <circle cx="60" cy="60" class="k-circle"></circle>
            </svg>
        </b:canvas>
    </div>
</Animate>
