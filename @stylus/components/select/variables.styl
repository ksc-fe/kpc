@require('../../styles/themes/default.styl')

$select-width := unit(200 / 14, rem)
$select-height := unit($default-height / 14, rem)
$select-border := 1px solid $border-color
$select-bg-color := #fff
$select-padding-right := unit(30 / 14, rem)
$select-padding-left := unit(10 / 14, rem)
$select-focus-border := 1px solid $primary-color

$select-clear-margin-right := unit(5 / 14, rem)
$select-placeholder-color := $disabled-color 

// disabled
$select-disabled-color := $disabled-color
$select-disabled-bg-color := $disabled-bg-color
$select-disabled-border-color := $disabled-border-color

// group
$select-group-label-color := $disabled-color
$select-group-label-padding := unit(8 / 14, rem)

// multiple
$select-multiple-values-margin := unit(5 / 14, rem)
$select-multiple-tag-padding := unit(2 / 14, rem) unit(5 / 14, rem)
$select-multiple-tag-border-radius := $border-radius
$select-multiple-tag-margin := 0 unit(5 / 14, rem) unit(5 / 14, rem) 0
$select-multiple-tag-delete-icon-margin-left := unit(5 / 14, rem)
$select-multiple-tag-delete-icon-font-size := unit(20 / 14, rem)

// size
$select-small-height := unit($small-height / 14, rem)
$select-small-font-size := unit(12 / 14, rem)
$select-small-multiple-values-margin := unit(1 / 14, rem)
$select-small-multiple-tag-margin := 0 unit(2 / 14, rem) unit(1 / 14, rem) 0

$select-mini-height := unit($mini-height / 14, rem)
$select-mini-font-size := unit(12 / 14, rem)
$select-mini-multiple-values-margin := unit(1 / 14, rem)
$select-mini-multiple-tag-margin := 0 unit(2 / 14, rem) unit(1 / 14, rem) 0

// dropdown
$select-dropdown-max-height := unit(200 / 14, rem)
