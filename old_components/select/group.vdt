const {children, label, _card, className, key, ref, ...rest} = self.get();

<div class={{ {"k-group": true, [className]: className} }} {{ ...rest }}>
    <div class="k-label" v-if={{ !_card }}>
        <b:label>{{ label }}</b:label>
    </div>
    {{ children }}
</div>
