const Link = require('components/link');

<ul>
    <li v-for={{ ['button', 'dialog', 'checkbox', 'radio', 'table', 'progress', 'slider'] }}>
        <Link href={{ `/components/${value}` }}>{{ value }}</Link>
    </li>
</ul>
