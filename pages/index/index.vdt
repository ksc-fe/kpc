const Link = require('components/link');

<ul>
    <li v-for={{ ['button', 'dialog', 'checkbox', 'radio', 'table', 'progress', 'slider','spinner','select', 'tooltip'] }}>
        <Link href={{ `/components/${value}` }}>{{ value }}</Link>
    </li>
</ul>
